services:
  claude-proxy:
    build: .
    ports:
      - "5066:5066"
    volumes:
      - ${CLAUDE_PROXY_DIR:-~/.claude/proxy}:/data
    environment:
      - WORKSPACE_PATH=/data
      - LOG_TOKEN_FORMAT=true
    restart: unless-stopped
